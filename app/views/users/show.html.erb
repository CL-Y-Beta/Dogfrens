<div class="container">
  <div class="row justify-content-center">
    <div class="col-sm-4">
      <div class="my-profile-pic text-center">
        <img class="rounded-circle shadow-4-strong" alt="avatar2" src="https://mdbcdn.b-cdn.net/img/new/avatars/1.webp" />
        <h3 class="text-center fs-5"><%= current_user.username %></h3>
        <h2 class="text-center fs-6">Ratings</h2> <!--- how to calculate this ratings-->
      </div>
    </div>
    <div class="col-md-8">
    <%= simple_form_for(current_user) do |f| %> <!--- to change to icon with Edit your profile -->
      <% link_to edit_user_path(current_user) do %>
        <%= f.button :submit, "Edit Your Profile", class: "btn btn-primary" %>
      <% end %>
    <% end %>
      <h2>About <%= current_user.username %></h2>
      <p><%= current_user.description %></p>
      <hr>
      <h2>Listings</h2>
        <div class="row">
          <div class="col-sm-4">
            <div class=card>
              <div class="card-body">
                <h5 class="card-title">Listing Title</h5> <!--- How do I get the listing title--->
                <p class="card-text">Listing Description</p>
              </div>
            </div>
          </div>
          <div class="col-sm-4">
            <div class=card>
              <div class="card-body">
                <h5 class="card-title">Listing Title</h5> <!--- How do I get the listing title--->
                <p class="card-text">Listing Description</p>
              </div>
            </div>
          </div>
          <div class="col-sm-4">
            <div class=card>
              <div class="card-body">
                <h5 class="card-title">Listing Title</h5> <!--- How do I get the listing title--->
                <p class="card-text">Listing Description</p>
              </div>
            </div>
          </div>
        </div>
    </div>
</div>

<%= simple_form_for(current_user) do |f| %> <!--- to change to icon with Edit your profile -->
    <% link_to edit_user_path(current_user) do %>
      <%= f.button :submit, "Edit Your Profile", class: "btn btn-primary" %>
    <% end %>
<% end %>
